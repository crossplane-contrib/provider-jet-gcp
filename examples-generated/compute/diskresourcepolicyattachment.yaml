apiVersion: compute.gcp.jet.crossplane.io/v1alpha1
kind: DiskResourcePolicyAttachment
metadata:
  name: example
spec:
  forProvider:
    disk: test-disk
    name: policy
    zone: us-central1-a
  providerConfigRef:
    name: example
